<%= form_for(:board, @game_board, :url => { :action => "update" }, :html => { :method => "put"}) do |f| %>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
    <div class="field">
    <%= f.label :board_size %><br />
    <%= f.text_field :board_size %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
